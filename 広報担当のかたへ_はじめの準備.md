# はじめの準備（1回だけ・パソコン1台につき1回）

Spark のデジタル版を、ご自身のパソコンから出せるようにするための準備です。
**1回やれば、次からは「公開する.command」をダブルクリックするだけ**になります。

30分ほどみておいてください。わからなくなったら、途中でも立川に聞いてください。

対象： **Mac**（Windows は別のやり方になります。立川にご相談ください）

---

## 【1】 道具を入れる

Mac の「ターミナル」というアプリを開きます。
（画面右上の虫めがねを押して `ターミナル` と打つと出てきます）

黒い画面が出たら、下の1行をコピーして貼り付け、`return` キーを押してください。
**途中でパスワード（Mac のログインパスワード）を聞かれたら入れてください。
打っても画面には何も出ませんが、入力はされています。**

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

終わったら、続けてこの1行。

```bash
brew install node git poppler gh
```

しばらく（5〜10分）かかります。文字がたくさん流れますが、そのまま待ってください。

---

## 【2】 Spark のデータを取ってくる

同じターミナルの画面で、この1行。

```bash
gh auth login
```

聞かれたら、こう選んでください（矢印キーで選んで `return`）。

| 聞かれること | 選ぶもの |
| --- | --- |
| What account do you want to log into? | **GitHub.com** |
| What is your preferred protocol? | **HTTPS** |
| Authenticate Git with your GitHub credentials? | **Yes** |
| How would you like to authenticate? | **Login with a web browser** |

8文字の英数字（ワンタイムコード）が出るので**メモして** `return` を押すと、
ブラウザが開きます。そこにコードを入れて、GitHub にログインしてください。

> GitHub のアカウントは立川が用意します。まだ持っていない場合は先に立川へ。

ログインできたら、次の1行でデータを取ってきます。

```bash
mkdir -p ~/Spark && cd ~/Spark && git clone https://github.com/dj77tac-ai/miraicco-spark.git && echo "できました"
```

「できました」と出れば成功です。

---

## 【3】 パスワードのファイルを作る

Spark を暗号化するためのパスワードです。**立川から直接聞いてください**
（メールやチャットに書かないでください）。

`ここにパスワード` の部分を、聞いたパスワードに書き換えてから実行します。

```bash
printf 'ここにパスワード' > ~/Spark/miraicco-spark/password.txt
```

> このファイルは、あなたのパソコンの中だけに置かれます。
> インターネットには出ません（出ない仕組みになっています）。

---

## 【4】 Google ドライブをパソコンに映す

「Google ドライブ（パソコン版）」が入っていて、
**`WORK / ★SBフォルダ / ★共有フォルダ`** が見える状態にしてください。

Finder の左側に「Google Drive」が出ていて、その中をたどって

```
マイドライブ / WORK / ★SBフォルダ / ★共有フォルダ / 99お知らせ / ブログ関連 / Spark
```

が開ければ大丈夫です。開けない場合は、立川に共有をお願いしてください。

---

## 【5】 動くか試す

Finder で `書類` …ではなく、**ホーム > Spark > miraicco-spark** フォルダを開きます。
（ターミナルで `open ~/Spark/miraicco-spark` と打つと、そのフォルダが開きます）

中の **`公開する.command`** をダブルクリックしてください。

- 「開発元が未確認のため開けません」と出たら
  → `公開する.command` を **右クリック → 開く** → 「開く」を押す（1回目だけ）
- 黒い画面が出て、号の一覧がずらっと出れば成功です
- 最後に「公開してよいですか？」と聞かれたら、**`no` と入れて終了**してください
  （試しなので、公開しなくて大丈夫です）

ここまでできたら準備は完了です。おつかれさまでした。

---

## こまったとき

| 出てきた文字 | どうするか |
| --- | --- |
| `★ 次のものが入っていません` | 【1】をもう一度 |
| `★ password.txt がありません` | 【3】をもう一度 |
| `★ Google ドライブのフォルダが見つかりません` | 【4】を確認。ドライブアプリが動いているか見る |
| `★ 公開に失敗しました` | インターネットにつながっているか確認。だめなら画面をそのまま立川へ |

画面に出ている文字をそのままコピーして立川に送っていただければ、原因がわかります。
