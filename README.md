# Spark 電子ブック

みらいっこの月刊誌 **Spark** の PDF を、保護者がスマートフォンやパソコンで
「ページをめくるように」読めるようにする仕組み。

- 公開URL： https://dj77tac-ai.github.io/miraicco-spark/
- リポジトリ： `dj77tac-ai/miraicco-spark`

有料サービス（FlipBuilder・年 $99）の代わりに作ったもの。
**費用はかからず、支払いが止まって突然読めなくなることもない。**

---

## 何をしているのか（しくみ）

1. PDF を1ページずつ画像に変える
2. その画像を**合言葉で暗号化**して `docs/d/` に置く
3. 置き場所（GitHub Pages）は誰でもアクセスできるが、
   ファイルの中身は合言葉なしでは**ただの意味のないデータ**にしか見えない
4. 保護者が合言葉を入れると、**そのブラウザの中だけで**画像に戻して表示する

サーバー側に仕掛けが要らないため、無料の置き場所だけで動く。

### 大事なこと

- **合言葉は暗号化そのものに使われている。** 画面をごまかして通り抜ける、
  ということができない作りになっている
- ただし、暗号化されたファイル自体は誰でも手に入る。
  **合言葉が短かったり、よくある単語だったりすると、時間をかければ破られる。**
  12文字以上・辞書に無い言葉を混ぜたものにすること
- 合言葉を変えると**全部作り直し**になる（`build.mjs` が自動で気づいて作り直す）

---

## ファイルの置き場所

| 場所 | 中身 |
| --- | --- |
| `config.json` | 号の一覧（**並び順もここで決まる**）と、元PDFを探すフォルダ |
| `password.txt` | 合言葉。**git に入らない**（`.gitignore` 済み） |
| `build.mjs` | PDF → 画像 → 暗号化 をする道具 |
| `docs/` | **これが公開される。** 画面のプログラムと暗号化ずみのデータ |
| `docs/d/` | 暗号化された本文（`build.mjs` が作る） |
| `work/` | 変換の途中で使う場所。作業後に自動で消える |
| `.buildstate.json` | 前回どの合言葉で作ったかの控え。手元だけ・git に入らない |

> `docs` という名前なのは、GitHub Pages が「リポジトリ直下」か「docs フォルダ」しか
> 公開できないため。説明書ではなく**公開されるサイト本体**が入っている。

---

## 広報チームからの受け取り方

元の PDF は、Google ドライブのこのフォルダに入れてもらう。

```
マイドライブ / Spark電子ブック_元データ /
```

- 同じフォルダに「はじめにお読みください.txt」を置いてある（ファイル名の付け方など）
- ファイル名は `Spark202510.pdf` のように **Spark + 年月**
- **ここに入れただけでは公開されない。** 下の手順で作り直して push したときに公開される

---

## 新しい号を出すとき

1. 広報チームが入れた PDF が、上のドライブのフォルダにあることを確認する
2. `config.json` の `issues` に**いちばん下へ**1行足す

   ```json
   { "id": "202510", "label": "2025年10月号", "pdf": "Spark202510.pdf" }
   ```

   - `id` … URL に使う名前（半角英数字）。**先頭4桁が年**として一覧の見出しに使われる
   - `label` … 一覧に出る名前
   - `pdf` … ファイル名だけでよい（`config.json` の `pdfDirs` を順に探す）

3. 作り直す（**すでに作った号はそのまま使うので数秒で終わる**）

   ```bash
   cd "/Users/tachikawaakane/Downloads/みらいっこDB/Spark電子ブック" && node build.mjs
   ```

4. 公開する

   ```bash
   cd "/Users/tachikawaakane/Downloads/みらいっこDB/Spark電子ブック" && git add -A && git commit -m "2025年10月号を追加" && git push
   ```

   push すると1〜2分で反映される。

---

## 並び順を変えたいとき

**`config.json` の `issues` の並びが、そのまま順番になる。**

- 上ほど古い号、**いちばん下が最新号**
- いちばん下の号が、一覧のいちばん上に「最新号」として大きく出る
- 残りは新しい順にならび、`id` の先頭4桁の年ごとにまとめて表示される
- 号の名前（`label`）を直したいときも、ここを書き換えて `node build.mjs` → `git push`

並べ替えや名前の変更だけなら、PDF を作り直さないので数秒で終わる。

---

## 手元で見て確かめる

```bash
cd "/Users/tachikawaakane/Downloads/みらいっこDB/Spark電子ブック/docs" && python3 -m http.server 8765
```

ブラウザで `http://localhost:8765` を開く。
（`file://` で直接開くと、ブラウザの決まりで暗号を戻せないため動かない）

---

## 必要なもの

- `pdftoppm` / `pdfinfo`（PDF を画像にする道具）
  ```bash
  brew install poppler
  ```
- Node.js（18 以上）

---

## 画面の作りかた

- `docs/index.html` … 3つの画面（合言葉／一覧／読む）
- `docs/app.js` … 合言葉の確認・暗号を戻す・ページめくり
- `docs/style.css` … 見た目。色は先頭の `:root` にまとめてある
- `docs/vendor/page-flip.*` … ページめくりの部品（[page-flip](https://github.com/Nodlik/StPageFlip) 2.0.7・MIT ライセンス）

`app.js` や `style.css` を直したら `node build.mjs` を実行すること。
`index.html` の中の番号が付け替わり、保護者のブラウザが古いものを使い続けるのを防ぐ。

### page-flip に1か所だけ手を入れてある

もとの page-flip は、毎回いちばん下を**白でぬりつぶして**から紙を描いていた。
そのため表紙を1枚だけ出すとき、**隣に白い紙が見えてしまう**。
`docs/vendor/page-flip.browser.js` の `clear()` を「透明でぬる」に変えて直してある
（ファイルの先頭にも書いてある）。**部品を新しくするときは、この変更も入れ直すこと。**

---

## 決めたこと・注意

- **画像は 150dpi・JPEG 品質 82。** A4 で1ページ約 400KB。
  もっときれいにしたい場合は `config.json` の `dpi` を上げる（そのぶん重くなる）
- **表紙と裏表紙は1枚だけで出し、画面の中央に置く**（`app.js` の `centerLonePage`）
- **合言葉は端末におぼえさせている**（`localStorage`）。
  一度入れれば次からは入れ直さなくてよい。「とじる」を押すと忘れる
- 検索に出ないよう `robots.txt` と `noindex` を入れてある
- ぜんぶ作り直したいときは `node build.mjs --force`
